/*import React from 'react';


function CharContainer( {characters }){
    return (
        <div className="char-container">
            {characters.map((submitCharcter) => (
                <div key={submitCharacter.id}>
                    <CharCard
                    submitCharacter={submitCharcter} 
                    />
                </div>
            ))}
        </div>
    )

}

export default CharContainer;*/